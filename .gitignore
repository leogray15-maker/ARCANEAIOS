# deps / builds
node_modules/
dist/
.vercel/
.DS_Store

# secrets — never commit
.env
.env.*
!.env.example
*service-account*.json
*firebase-adminsdk*.json
serviceAccountKey.json

# regenerable data
data/archives/index.json
data/archives/modules/

# derived from the brain at build time
apps/facility/public/brain.json

# HERALD daemon log
data/herald-auto.log

# HERALD staging (drafts before they are emitted into the brain)
.herald-staging/

# Obsidian workspace state (per-device); keep app/core-plugins config
brain/.obsidian/workspace*.json
brain/.obsidian/cache
